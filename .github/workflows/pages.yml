name: Deploy Stoppclock to GitHub Pages

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

env:
  NODE_VERSION: 22
  CUSTOM_DOMAIN: www.stoppclock.com
  TARGET_URL: https://www.stoppclock.com/

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (pinned)
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8  # why: policy (SHA-pinned)

      - name: Setup Node (pinned)
        uses: actions/setup-node@2028fbc5c25fe9cf00d9f06a71cc4710d4507903
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Build
        run: npm run build

      - name: Add CNAME and .nojekyll  # why: custom domain & disable jekyll
        run: |
          echo "${{ env.CUSTOM_DOMAIN }}" > ./dist/CNAME
          touch ./dist/.nojekyll

      - name: Upload Pages artifact (pinned)
        uses: actions/upload-pages-artifact@7b1f4a764d45c48632c6b24a0339c27f5614fb0b
        with:
          path: ./dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        name: Deploy to GitHub Pages (pinned)
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e

  postcheck:
    needs: deploy
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Setup Node (pinned)
        uses: actions/setup-node@2028fbc5c25fe9cf00d9f06a71cc4710d4507903
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install Playwright (Chromium only)
        run: |
          npm init -y >/dev/null 2>&1
          npm i -D playwright@1.47.2
          npx playwright install --with-deps chromium

      - name: "Write smoke test (hardened: pageerror fail, 3P console warn, SW cache check)"
        run: |
          cat > smoke.mjs <<'EOF'
          import { chromium } from 'playwright';
          import fs from 'fs/promises';
          import path from 'path';

          const base = (process.env.TARGET_URL || 'https://www.stoppclock.com').replace(/\/+$/,'');
          const host = new URL(base).host;
          const pathsToCheck = ['/', '/manifest.webmanifest', '/imprint.html', '/sw.js'];
          const artifactsDir = path.resolve('artifacts/playwright');
          const consoleOwnErrors = [];
          const pageErrors = [];
          const navTimeout = 30000;

          async function ensureDir() { await fs.mkdir(artifactsDir, { recursive: true }); }
          async function saveShot(page, name) { await ensureDir(); await page.screenshot({ path: path.join(artifactsDir, `${name}.png`), fullPage: true }); }
          function slugify(s){ return s.replace(/[^a-z0-9]+/gi,'-').replace(/^-+|-+$/g,'').toLowerCase() || 'root'; }
          function log(msg){ console.log(`[SMOKE] ${msg}`); }
          function warn(msg){ console.warn(`[SMOKE][WARN] ${msg}`); }

          // why: 3rd-party noise shouldn't fail deploys
          const allowedConsole = [
            'net::ERR_BLOCKED_BY_CLIENT',
            'adsbygoogle', 'googletagservices', 'googletagmanager',
            'gtag/js?id=',
            'cookieconsent',
          ];

          (async () => {
            const browser = await chromium.launch();
            const context = await browser.newContext({ ignoreHTTPSErrors: true });
            const page = await context.newPage();

            page.on('console', (m) => {
              const type = m.type();
              const text = (m.text && m.text()) || '';
              if (type !== 'error') return;
              if (allowedConsole.some(w => text.includes(w))) return;
              // why: only fail on own app errors (URLs to our host or built assets)
              const isOwn = text.includes(host) || text.includes('/assets/') || text.includes('/sw.js');
              if (isOwn) consoleOwnErrors.push(text); else console.warn('[IGNORED 3P]', text);
            });

            page.on('pageerror', (err) => {
              pageErrors.push(String(err?.message || err));
            });

            for (const p of pathsToCheck) {
              const url = base + p;
              log(`Visiting ${url}`);
              const resp = await page.goto(url, { waitUntil: 'networkidle', timeout: navTimeout });
              const status = resp?.status() || 0;
              if (status !== 200) {
                await saveShot(page, `http-${status}-${slugify(p)}`);
                await browser.close();
                console.error(`[FAIL] HTTP ${status} for ${url}`);
                process.exit(2);
              }
              const content = await page.content();
              if (!content || content.length < 100) {
                await saveShot(page, `short-html-${slugify(p)}`);
                await browser.close();
                console.error(`[FAIL] Empty/short HTML at ${url}`);
                process.exit(3);
              }
            }

            // SW cache header (warn only)
            const head = await context.request.fetch(`${base}/sw.js`, { method: 'HEAD' });
            const cc = head.headers()['cache-control'] || head.headers()['Cache-Control'] || '';
            if (!cc) {
              warn('Cache-Control header for /sw.js is missing.');
            } else {
              const m = /max-age\s*=\s*(\d+)/i.exec(cc);
              const age = m ? parseInt(m[1], 10) : null;
              if (age === null) {
                warn(`Cache-Control for /sw.js unparsed: "${cc}"`);
              } else if (age > 300) {
                warn(`Cache-Control for /sw.js too long (max-age=${age}). Consider shorter caching.`);
              } else {
                log(`Cache-Control for /sw.js OK: ${cc}`);
              }
            }

            if (pageErrors.length) {
              await saveShot(page, `pageerror`);
              await browser.close();
              console.error('[FAIL] Uncaught page errors:\n' + pageErrors.join('\n---\n'));
              process.exit(4);
            }
            if (consoleOwnErrors.length) {
              await saveShot(page, `console-errors`);
              await browser.close();
              console.error('[FAIL] Console errors from own app:\n' + consoleOwnErrors.join('\n---\n'));
              process.exit(6);
            }

            await browser.close();
            log('All checks passed.');
          })().catch(async (e) => {
            console.error('[FAIL] Uncaught error in smoke:', e);
            process.exit(5);
          });
          EOF

      - name: Run smoke test
        env:
          TARGET_URL: ${{ env.TARGET_URL }}
        run: node smoke.mjs

      - name: Upload Playwright artifacts (screenshots)
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: playwright-screenshots
          path: artifacts/playwright/**
          if-no-files-found: ignore
          retention-days: 7
